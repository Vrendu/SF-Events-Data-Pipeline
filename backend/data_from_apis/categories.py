import re
import requests
import logging 


def determine_categories(title, description, venue, categories):
    text = f"{title} {description} {venue}".lower()
    print(f"Determining categories with text: '{text}'")
    

    


    return categories

